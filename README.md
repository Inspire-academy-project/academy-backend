| (학생 계정 없음)     | 원생은 계정 없이 등록 |# academy-backend
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |재수 학원 관리 웹 서비스의 API 서버. 출결 관리 · 일일4제 영상 · 학원비/급식비 관리.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |Express 5 + Prisma 7 + PostgreSQL + JWT
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |> 프론트엔드는 별도 저장소: [academy-frontend](https://github.com/Inspire-academy-project/academy-frontend)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 1. 준비물
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- Node.js 20 이상
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- pnpm — 없으면 `npm install -g pnpm`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- PostgreSQL — 로컬 설치 대신 [Supabase](https://supabase.com) 또는 [Neon](https://neon.tech) 무료 플랜 추천
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 2. 실행
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```bash
| (학생 계정 없음)     | 원생은 계정 없이 등록 |pnpm install
| (학생 계정 없음)     | 원생은 계정 없이 등록 |cp .env.example .env
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |`.env`에서 `DATABASE_URL`을 실제 DB 주소로, `JWT_SECRET`을 임의의 긴 문자열로 바꾼다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```bash
| (학생 계정 없음)     | 원생은 계정 없이 등록 |pnpm db:push    # 스키마를 DB에 반영 (첫 실행)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |pnpm db:seed    # 테스트 계정/데이터 생성
| (학생 계정 없음)     | 원생은 계정 없이 등록 |pnpm dev        # http://localhost:4000
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |시드 계정 — 비밀번호는 모두 `academy1234`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 이메일                | 역할          |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || --------------------- | ------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || admin@academy.kr      | 원장(ADMIN)   |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || teacher@academy.kr    | 강사(TEACHER) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || student1~3@academy.kr | 학생(STUDENT) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 3. API 목록
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |`/health`를 제외한 모든 경로는 `Authorization: Bearer <token>` 헤더가 필요하다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |### 인증 `/api/auth`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 메서드 | 경로        | 권한   | 설명                                     |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ------ | ----------- | ------ | ---------------------------------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/register` | 공개   | 학생 회원가입 (User + Student 동시 생성) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/login`    | 공개   | 로그인, JWT 발급 (7일 유효)              |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/me`       | 로그인 | 내 정보 조회                             |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |### 학생 `/api/students`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 메서드 | 경로   | 권한           | 설명                                              |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ------ | ------ | -------------- | ------------------------------------------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/`    | ADMIN, TEACHER | 목록 (`?className=`, `?active=`, `?q=` 이름 검색) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/:id` | ADMIN, TEACHER | 상세 + 최근 출결 30건/납부 12건                   |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/`    | ADMIN, TEACHER | 학생 등록                                         |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || PATCH  | `/:id` | ADMIN, TEACHER | 반·연락처·재원 여부 수정                          |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |### 출결 `/api/attendance`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 메서드 | 경로                  | 권한           | 설명                               |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ------ | --------------------- | -------------- | ---------------------------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/me`                 | 학생           | 내 출결 (`?from=&to=`)             |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/?date=YYYY-MM-DD`   | ADMIN, TEACHER | 그날 전체 명단 + 출결 상태         |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/student/:studentId` | ADMIN, TEACHER | 특정 학생 출결                     |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/`                   | ADMIN, TEACHER | 1명 출결 입력 (같은 날짜면 덮어씀) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/bulk`               | ADMIN, TEACHER | 여러 명 한 번에 입력               |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |상태값: `PRESENT`(출석) `LATE`(지각) `ABSENT`(결석) `EXCUSED`(인정결석)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |### 학원비 `/api/payments`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |학원비는 달력 월이 아니라 **16일~다음달 15일** 주기로 청구한다(예: "3월" = 3/16~4/15).
| (학생 계정 없음)     | 원생은 계정 없이 등록 |그래서 연·월이 아니라 시작일·종료일을 가진 `BillingPeriod`로 관리한다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 메서드 | 경로                    | 권한           | 설명                                         |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ------ | ----------------------- | -------------- | -------------------------------------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/periods`              | ADMIN, TEACHER | 청구 주기 목록 (`?feeType=`)                 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/periods`              | ADMIN          | 청구 주기 생성 (시작일·종료일·기준금액)      |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/periods/:id/generate` | ADMIN          | **재원생 전체 청구서 일괄 생성 + 일할계산**  |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/me`                   | 학생           | 내 납부 내역                                 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/`                     | ADMIN          | 전체 (`?periodId=&feeType=&status=&gender=`) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/unpaid`               | ADMIN          | **미납 목록 + 미납 총액**                    |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/`                     | ADMIN          | 청구 1건 생성                                |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || PATCH  | `/:id`                  | ADMIN          | 입금액 기록 → 상태 자동 계산                 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |상태값: `UNPAID`(미납) `PARTIAL`(부분납) `PAID`(완납) `EXEMPT`(면제)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |`/unpaid`는 **납부기한이 지난 건만** 집계한다. 아직 도래하지 않은 달까지 보려면
| (학생 계정 없음)     | 원생은 계정 없이 등록 |`?includeUpcoming=true`를 붙인다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |### 급식 `/api/meals`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |급식은 학원비와 결제 방식이 다르다. 전원이 먹지 않고, **선불 식권제**로 운영된다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |월 단위로 신청 개수를 받아 결제하고(단가 7,500원), 남은 개수는 다음 달로 이월된다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |점심·저녁을 따로 신청한다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 메서드 | 경로            | 권한           | 설명                                         |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ------ | --------------- | -------------- | -------------------------------------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/orders`       | ADMIN, TEACHER | 월별 신청·결제 현황 + 합계 (`?year=&month=`) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/orders`       | ADMIN          | 신청 등록 (결제 개수 = 신청 − 이월)          |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || PATCH  | `/orders/:id`   | ADMIN          | 결제 처리, 개수 수정                         |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/orders/me`    | 학생           | 내 급식 신청 내역                            |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/records`      | ADMIN, TEACHER | **그날의 급식수령표** (`?date=&mealType=`)   |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/records`      | ADMIN, TEACHER | 수령 체크 (`received:false`면 취소)          |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/usage`        | ADMIN, TEACHER | 월별 신청 대비 실제 수령 횟수, 잔여 개수     |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/service-days` | ADMIN          | 급식 미운영일 지정 (공휴일 등)               |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |`mealType`은 `LUNCH`(점심) 또는 `DINNER`(저녁).
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |### 영상 `/api/videos`
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 메서드 | 경로          | 권한           | 설명                                                          |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ------ | ------------- | -------------- | ------------------------------------------------------------- |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/`           | 로그인         | 목록 (`?date=&subject=`). 학생은 공개된 것만                  |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || POST   | `/`           | ADMIN, TEACHER | 업로드 (`multipart/form-data`, 파일 필드명 `video`, 최대 1GB) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || GET    | `/:id/stream` | 로그인         | 스트리밍 (Range 지원 → 브라우저에서 구간 이동 가능)           |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || PATCH  | `/:id`        | ADMIN, TEACHER | 제목·공개 여부 수정                                           |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 4. 데이터 모델
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```
| (학생 계정 없음)     | 원생은 계정 없이 등록 |User (로그인 계정)
| (학생 계정 없음)     | 원생은 계정 없이 등록 | ├──0:1── Student ──1:N── Attendance    (studentId + date 유니크)
| (학생 계정 없음)     | 원생은 계정 없이 등록 | │           ├──1:N── Payment           (studentId + periodId 유니크)
| (학생 계정 없음)     | 원생은 계정 없이 등록 | │           ├──1:N── MealOrder         (studentId + 연·월 + 점심/저녁 유니크)
| (학생 계정 없음)     | 원생은 계정 없이 등록 | │           └──1:N── MealRecord        (studentId + date + 점심/저녁 유니크)
| (학생 계정 없음)     | 원생은 계정 없이 등록 | └──1:N── Video
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |BillingPeriod ──1:N── Payment           (학원비/급식비, 16일~15일 주기)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |MealServiceDay                          (급식 미운영일)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- **User** 는 로그인 계정. `role` 로 원장/강사/학생 구분
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- **Student** 는 계정과 분리돼 있다. **학생 로그인 없이도 원생을 등록·관리할 수 있고**,
| (학생 계정 없음)     | 원생은 계정 없이 등록 |  나중에 학생 계정을 붙일 때 연결만 하면 된다 (`userId` 는 선택 항목)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- **`seatNo`(좌석번호)** 가 학원의 실질적 학생 식별자다. 학원비 명부·급식 신청·질의응답
| (학생 계정 없음)     | 원생은 계정 없이 등록 |  예약이 모두 이 번호를 쓴다
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- **유니크 제약** 덕분에 같은 날 출결 중복 입력, 같은 주기 학원비 중복 청구, 같은 날
| (학생 계정 없음)     | 원생은 계정 없이 등록 |  급식 중복 수령이 DB 차원에서 막힌다
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 5. 폴더 구조
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```
| (학생 계정 없음)     | 원생은 계정 없이 등록 |src/
| (학생 계정 없음)     | 원생은 계정 없이 등록 |├── index.ts           서버 진입점, 라우터 등록
| (학생 계정 없음)     | 원생은 계정 없이 등록 |├── lib/
| (학생 계정 없음)     | 원생은 계정 없이 등록 |│   ├── env.ts         환경변수 검증 (잘못되면 부팅 시 즉시 종료)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |│   ├── prisma.ts      Prisma 클라이언트 (pg 어댑터)
| (학생 계정 없음)     | 원생은 계정 없이 등록 |│   └── http-error.ts  에러 클래스, 날짜 파싱
| (학생 계정 없음)     | 원생은 계정 없이 등록 |├── middleware/
| (학생 계정 없음)     | 원생은 계정 없이 등록 |│   ├── auth.ts        JWT 검증, 역할별 권한 가드
| (학생 계정 없음)     | 원생은 계정 없이 등록 |│   └── error.ts       전역 에러 핸들러
| (학생 계정 없음)     | 원생은 계정 없이 등록 |└── routes/            auth · students · attendance · payments · meals · videos
| (학생 계정 없음)     | 원생은 계정 없이 등록 |```
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 6. 자주 쓰는 명령어
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || 명령어            | 설명                                       |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || ----------------- | ------------------------------------------ |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || `pnpm dev`        | 개발 서버 (파일 변경 시 자동 재시작)       |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || `pnpm db:push`    | 스키마 변경을 DB에 즉시 반영 (개발 초기용) |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || `pnpm db:migrate` | 마이그레이션 파일로 관리 (실제 운영용)     |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || `pnpm db:studio`  | 브라우저에서 DB 데이터 확인/편집           |
| (학생 계정 없음)     | 원생은 계정 없이 등록 || `pnpm typecheck`  | 타입 검사                                  |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |## 7. 알아둘 점
| (학생 계정 없음)     | 원생은 계정 없이 등록 |
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- **Prisma 7부터** DB 주소가 `schema.prisma`가 아니라 `prisma.config.ts`에 있다. 인터넷 자료 대부분은 6버전 기준이라 `url = env("DATABASE_URL")`을 스키마에 쓰라고 하는데, 7에서는 에러가 난다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- 영상은 현재 서버 로컬 `uploads/` 폴더에 저장된다. 실제 배포 시에는 AWS S3 같은 외부 스토리지로 옮겨야 한다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- 비밀번호는 bcrypt 해시로만 저장되며 평문은 어디에도 남지 않는다.
| (학생 계정 없음)     | 원생은 계정 없이 등록 |- `src/generated/`는 Prisma가 자동 생성하는 코드라 커밋하지 않는다. `pnpm install` 후 `pnpm db:generate`로 다시 만든다.
